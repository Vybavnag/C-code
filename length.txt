// A simple program to calculate the length of a line segment
#include <iostream>
#include <cmath>

using namespace std;

int main(){
    float x1, x2, y1, y2;
    cin>>x1>>y1>>x2>>y2;
    float length= sqrt(pow(x2-x1,2)+pow(y2-y1,2));
    cout<<length<<endl;
    return 0;
}