#include <iostream>
#include <string>

using namespace std;

int main (int argc, char* argv[]){

    int n = stoi(argv[3]);
    string p = argv[2];
    string s = argv[1];

    s.insert(n,p);
    cout<<s<<endl;
    return 0;
}
